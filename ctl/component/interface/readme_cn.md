# 控制器接口组件

[English](readme.md) | **简体中文**

本目录定义面向控制算法的 ADC、PWM、DAC、调制、偏置、增益和 GMP SIL 接口，将可复用控制逻辑与具体平台的外设寄存器隔离。

应选择能够表达目标信号链的最小接口，在初始化阶段绑定平台实现，并避免在通用控制模块中直接访问目标寄存器。高级 PWM 与调制接口仍必须遵守目标硬件的极性、死区和保护约束。
