# GMP 外设模拟器

[English](readme.md) | **简体中文**

本目录用于保存 GMP 测试和 SIL 流程中的主机侧外设模拟实现。新增实现时应说明被模拟外设的接口契约、时序模型、传输方式、支持平台以及负责验证它的测试或 suite。

模拟器不能依赖仅在目标芯片存在的头文件，也不能写入仓库绝对路径。共享资源通过 `GMP_PRO_LOCATION` 定位，可复用协议逻辑应与 GUI 或操作系统适配层分离。
