# Verilog controller template placeholder

This directory is reserved for a future Verilog controller runtime/template library. It currently contains no implementation and is not registered as a buildable GMP module. Existing experimental RTL remains in the sibling `components`, `math_blocks`, `bus_driver`, and `utilities` directories.
