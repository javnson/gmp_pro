/**
 * @file gmp_type.h
 * @brief GMP standard configuration and portable type entry point.
 *
 * Library components that only need GMP types, compiler attributes, endian
 * helpers, and error-code definitions should include this header instead of
 * the full runtime entry point in gmp_core.h.
 */

#ifndef GMP_TYPE_H
#define GMP_TYPE_H

#include <core/std/gmp.std.h>

#endif // GMP_TYPE_H
